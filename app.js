'use strict';
const $=s=>document.querySelector(s), app=$('#app');
const KEY='ypp-prep-v2', SESSION='ypp-session-v2';
function read(key,fallback){try{return JSON.parse(localStorage.getItem(key))??fallback}catch{return fallback}}
let state=read(KEY,{items:{},history:[],days:[]});
if(!state || typeof state.items!=='object'||!Array.isArray(state.history)||!Array.isArray(state.days))state={items:{},history:[],days:[]};
let session=null,timer=null,view='dashboard';
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function persist(key,value){try{localStorage.setItem(key,JSON.stringify(value))}catch{$('#storageNotice').hidden=false}}
const save=()=>persist(KEY,state),saveSession=()=>persist(SESSION,session);
const pct=(a,b)=>b?Math.round(a/b*100):0;
function shuffle(xs){const a=[...xs];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}
const sourceLink=id=>SOURCES[id]?`<a href="${SOURCES[id].url}" target="_blank" rel="noopener noreferrer">${esc(SOURCES[id].title)} ↗</a>`:'';
function stats(d){const qs=QUESTIONS.filter(q=>!d||q.d===d),seen=qs.filter(q=>state.items[q.id]);return {seen:seen.length,correct:seen.filter(q=>state.items[q.id].ok).length,total:qs.length}}
function actions(){return `<div class="actions"><button class="btn" onclick="startDiagnostic()">Faire mon diagnostic</button><button class="ghost" onclick="showView('exam')">Examen blanc · 45 min</button></div>`}
function dashboard(){const s=stats();app.innerHTML=`<div class="hero"><div><div class="eyebrow">SESSION 2026 · PRÉPARATION PERSONNELLE</div><h2>Prêt pour les quatre domaines confirmés ?</h2><p>Commence par 20 questions de diagnostic. La version du 23 septembre retire les exercices de calcul : le raisonnement est désormais logique, analytique et non numérique.</p>${actions()}</div><div class="scorebox"><small>DERNIÈRES RÉPONSES</small><div class="score">${s.seen?pct(s.correct,s.seen):'—'}<small>${s.seen?'%':''}</small></div><small>${s.seen} / ${s.total} questions différentes travaillées<br>Indicateur d’entraînement, pas une probabilité de sélection.</small></div></div><div class="notice"><b>Mise à jour confirmée · 23 septembre :</b> QCM uniquement · 45 minutes sans interruption · aucune réponse rédigée · aucune composante mathématique · calculatrice non nécessaire · français ou anglais · fenêtre du 28 septembre au 5 octobre 2026, 23 h 59 GMT. Le nombre de questions, leur répartition et le seuil restent non communiqués.</div><div class="grid4">${Object.entries(DOMAINS).map(([d,label])=>{const s=stats(d);return `<button class="card module-button" onclick="startPractice('${d}')"><small>${label}</small><div class="num">${s.seen?pct(s.correct,s.seen)+'%':'—'}</div><small>${s.seen}/${s.total} questions explorées</small><p>Travailler ce domaine →</p></button>`}).join('')}</div><div class="section-title"><h2>Ta prochaine séance</h2></div><div class="panel"><p>Une séance ciblée privilégie les erreurs et les questions encore inédites. Les résultats restent sur cet appareil ; exporte-les pour les conserver.</p><button class="btn" onclick="startAdaptive()">Séance recommandée · 10 questions</button> <button class="ghost" onclick="showView('plan')">Mon programme de 12 séances</button></div><div class="section-title"><h2>Banque et actualités</h2><span class="muted">Édition du 23 septembre 2026</span></div><div class="knowledge">${NEWS.slice(0,2).map(n=>`<article><small>${n.date}</small><h3>${n.title}</h3><p>${n.body}</p>${sourceLink(n.source)}</article>`).join('')}</div>`}
function learn(){app.innerHTML=`<p class="muted">12 fiches de révision · français, avec lexique anglais. Clique sur une fiche, puis explique-la sans regarder.</p><div class="knowledge">${LESSONS.map(([title,d,body,source])=>`<details><summary>${esc(title)}</summary><p>${esc(body)}</p>${sourceLink(source)}<p><button class="ghost" onclick="startPractice('${d}')">M’entraîner sur ce domaine</button></p></details>`).join('')}</div>`}
function news(){app.innerHTML=`<div class="notice"><b>Veille éditoriale du 16 septembre 2026.</b> Sélection de sources retrouvées, sans actualisation automatique ni garantie d’exhaustivité. Les pages complètes de certaines sources n’étaient pas accessibles lors de la vérification ; les fiches s’en tiennent aux informations visibles dans leurs résultats officiels. Consulte les publications nouvelles avant le test.</div><div class="knowledge">${NEWS.map(n=>`<article><span class="tag">${n.date}</span><h3>${n.title}</h3><p>${n.body}</p><div class="explain"><b>Question de réflexion</b><p>${n.prompt}</p></div><p>${sourceLink(n.source)}</p></article>`).join('')}</div><div class="panel"><h3>Actualités à vérifier avant le 28 septembre</h3><p>Privilégie les publications de la BAD sur les opérations, Mission 300, le FAD, les perspectives économiques et les pays africains. Pour chaque nouvelle publication, note date, instrument, cible, état d’avancement et lien avec les High 5.</p>${sourceLink('news')}</div>`}
function practiceHome(){app.innerHTML=`<div class="panel"><h2>Choisis ton entraînement</h2><p>Corrections immédiatement après validation, ou à la fin d’un examen blanc. Questions originales ; aucun exercice de calcul. Les cas de raisonnement portent sur logique, déduction, causalité, cohérence et lecture critique.</p>${actions()}<p><button class="ghost" onclick="startAdaptive()">Recommandé · 10 questions</button> <button class="ghost" onclick="startErrors()">Reprendre mes erreurs</button></p></div><div class="grid4">${Object.entries(DOMAINS).map(([d,label])=>`<button class="card module-button" onclick="startPractice('${d}')"><h3>${label}</h3><p>${QUESTIONS.filter(q=>q.d===d).length} questions disponibles</p><span>Commencer →</span></button>`).join('')}</div>`}
function balanced(n){return shuffle(Object.keys(DOMAINS).flatMap(d=>shuffle(QUESTIONS.filter(q=>q.d===d)).slice(0,n)))}
function prioritize(qs){return shuffle(qs).sort((a,b)=>{const rank=q=>!state.items[q.id]?1:state.items[q.id].ok?2:0;return rank(a)-rank(b)})}
function startDiagnostic(){startQuiz(balanced(5),false,'Diagnostic')}
function startPractice(d){startQuiz(prioritize(QUESTIONS.filter(q=>q.d===d)).slice(0,10),false,DOMAINS[d])}
function startAdaptive(){const d=Object.keys(DOMAINS).sort((a,b)=>{const x=stats(a),y=stats(b);return (x.seen?x.correct/x.seen:0)-(y.seen?y.correct/y.seen:0)})[0];startPractice(d)}
function startErrors(){const qs=QUESTIONS.filter(q=>state.items[q.id]&&!state.items[q.id].ok);if(!qs.length){app.innerHTML='<div class="panel"><h2>Aucune erreur à reprendre</h2><p>Fais un diagnostic pour identifier tes points à travailler.</p>'+actions()+'</div>';return}startQuiz(shuffle(qs).slice(0,20),false,'Reprise des erreurs')}
function startExam(){const n=Number($('#examSize').value);startQuiz(balanced(n/4),true,'Examen blanc')}
function startQuiz(qs,exam,title){clearInterval(timer);session={id:Date.now()+'-'+Math.random(),qs:qs.map(q=>({ ...q,order:shuffle(q.o.map((_,i)=>i))})),answers:{},checked:{},flagged:{},times:{},i:0,exam,title,start:Date.now(),deadline:exam?Date.now()+2700000:null,entered:Date.now()};saveSession();renderQ();clock();}
function remaining(){return Math.max(0,Math.ceil((session.deadline-Date.now())/1000))}
function time(s){return `${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`}
function clock(){clearInterval(timer);if(!session?.exam)return;timer=setInterval(()=>{if(!session)return;const s=remaining();if($('#timer'))$('#timer').textContent=time(s);if(!s)finish(true)},500)}
function track(){if(!session)return;session.times[session.i]=(session.times[session.i]||0)+Math.max(0,Date.now()-session.entered);session.entered=Date.now()}
function renderQ(){const q=session.qs[session.i],sel=session.answers[session.i],checked=session.checked[session.i];$('#pageTitle').textContent=session.title;app.innerHTML=`<div class="quiz"><div class="qtop"><span>${DOMAINS[q.d]}</span><span>${session.exam?`<b id="timer" role="timer">${time(remaining())}</b> · `:''}Question ${session.i+1}/${session.qs.length}</span></div><progress max="${session.qs.length}" value="${Object.keys(session.answers).length}" aria-label="Questions renseignées"></progress><h2 class="question" tabindex="-1">${esc(q.fr)}</h2>${q.table||''}<div class="options" role="group" aria-label="Réponses">${q.order.map((original,pos)=>`<button class="option ${sel===original?'selected':''} ${checked&&original===q.a?'correct':''} ${checked&&sel===original&&sel!==q.a?'wrong':''}" aria-pressed="${sel===original}" ${checked?'disabled':''} onclick="selectOpt(${original})">${String.fromCharCode(65+pos)}. ${esc(q.o[original])}</button>`).join('')}</div>${checked?`<div class="explain" role="status"><b>${sel===q.a?'✓ Bonne réponse':'À revoir'}</b><p>${esc(q.x)}</p>${sourceLink(q.source)}</div>`:''}<div class="quizfoot"><button class="ghost" ${session.i===0?'disabled':''} onclick="move(-1)">Précédente</button><button class="ghost" aria-pressed="${!!session.flagged[session.i]}" onclick="flag()">${session.flagged[session.i]?'★ Marquée':'☆ À revoir'}</button>${!session.exam&&!checked?`<button class="btn" ${sel===undefined?'disabled':''} onclick="checkAnswer()">Vérifier</button>`:`<button class="btn" onclick="move(1)">${session.i===session.qs.length-1?'Voir le bilan':'Suivante'}</button>`}</div><div class="question-map" aria-label="Navigation entre questions">${session.qs.map((_,i)=>`<button class="${i===session.i?'current':''} ${session.answers[i]!==undefined?'answered':''}" aria-label="Question ${i+1}${session.flagged[i]?', marquée':''}" onclick="jump(${i})">${i+1}${session.flagged[i]?'★':''}</button>`).join('')}</div><div class="quizfoot"><small>Les réponses peuvent être modifiées avant validation${session.exam?' finale':''}.</small><button class="ghost" onclick="finish(false)">Terminer la séance</button></div></div>`}
function selectOpt(i){if(session.checked[session.i])return;session.answers[session.i]=i;saveSession();renderQ()}
function checkAnswer(){if(session.answers[session.i]===undefined||session.checked[session.i])return;track();session.checked[session.i]=true;saveSession();renderQ()}
function flag(){session.flagged[session.i]=!session.flagged[session.i];saveSession();renderQ()}
function jump(i){track();session.i=i;saveSession();renderQ();$('.question').focus()}
function move(dir){if(dir===1&&session.i===session.qs.length-1){finish(false);return}jump(session.i+dir)}
function finish(auto=false){if(!session)return;if(!auto&&!confirm('Terminer cette séance ? Les questions sans réponse compteront comme incorrectes.'))return;track();clearInterval(timer);const total=session.qs.length,correct=session.qs.filter((q,i)=>session.answers[i]===q.a).length;const result={id:session.id,title:session.title,date:new Date().toISOString(),exam:session.exam,total,correct,elapsed:Math.round((Date.now()-session.start)/1000),rows:session.qs.map((q,i)=>({id:q.id,sel:session.answers[i]??null,seconds:Math.round((session.times[i]||0)/1000)}))};if(!state.history.some(h=>h.id===result.id)){result.rows.forEach(r=>{if(r.sel!==null){const q=QUESTIONS.find(q=>q.id===r.id);state.items[r.id]={ok:r.sel===q.a,attempts:(state.items[r.id]?.attempts||0)+1,date:result.date}}});state.history.unshift(result);state.history=state.history.slice(0,40);save()}session=null;saveSession();renderResult(result,auto)}
function renderResult(r,auto=false){$('#pageTitle').textContent='Bilan de séance';app.innerHTML=`<div class="panel result"><span class="tag">${auto?'TEMPS ÉCOULÉ':'SÉANCE TERMINÉE'}</span><div class="big-result">${pct(r.correct,r.total)}%</div><h2>${r.correct} / ${r.total} bonnes réponses</h2><p>${r.rows.filter(x=>x.sel===null).length} sans réponse · ${time(r.elapsed)} écoulées</p><p class="muted">Score d’entraînement uniquement. Le seuil et le barème officiels ne sont pas connus.</p><button class="btn" onclick="startErrors()">Reprendre mes erreurs</button> <button class="ghost" onclick="showView('progress')">Ma progression</button></div><div class="grid4">${Object.entries(DOMAINS).map(([d,label])=>{const rows=r.rows.filter(x=>QUESTIONS.find(q=>q.id===x.id)?.d===d);return `<div class="card"><small>${label}</small><div class="num">${rows.length?pct(rows.filter(x=>x.sel===QUESTIONS.find(q=>q.id===x.id).a).length,rows.length)+'%':'—'}</div></div>`}).join('')}</div><h2>Correction complète</h2>${r.rows.map((row,i)=>{const q=QUESTIONS.find(q=>q.id===row.id);if(!q)return '';return `<details ${row.sel!==q.a?'open':''}><summary>${i+1}. ${row.sel===q.a?'✓':'À revoir ·'} ${esc(q.fr)}</summary><p><b>Ta réponse :</b> ${row.sel===null?'Sans réponse':esc(q.o[row.sel])}</p><p><b>Bonne réponse :</b> ${esc(q.o[q.a])}</p><p>${esc(q.x)}</p><small>${row.seconds} s sur cette question · </small>${sourceLink(q.source)}</details>`}).join('')}`}
function examHome(){app.innerHTML=`<div class="hero"><div><span class="tag">EXAMEN BLANC</span><h2>45 minutes. QCM. Aucun calcul.</h2><p>Corrections à la fin, questions et choix mélangés. Le raisonnement est non numérique. Le chronomètre continue si tu changes d’onglet ou actualises la page.</p><label for="examSize">Format d’entraînement</label><select id="examSize"><option value="40">40 questions · 10 par domaine</option><option value="60">60 questions · 15 par domaine</option></select><p>Répartition pédagogique, sans présumer du format officiel.</p><button class="btn gold" onclick="startExam()">Commencer · 45:00</button></div></div><div class="notice"><b>En conditions réelles :</b> ordinateur conseillé, connexion stable, calme et 45 minutes disponibles. Aucune calculatrice n’est nécessaire. Cette plateforme sert uniquement avant le test ; l’invitation interdit l’IA, les ressources externes et l’assistance pendant l’évaluation officielle.</div>`}
function progress(){const s=stats();app.innerHTML=`<div class="grid4"><div class="card"><small>Questions différentes</small><div class="num">${s.seen}/${s.total}</div></div><div class="card"><small>Dernières réponses justes</small><div class="num">${s.seen?pct(s.correct,s.seen)+'%':'—'}</div></div><div class="card"><small>Examens enregistrés</small><div class="num">${state.history.filter(h=>h.exam).length}</div></div><div class="card"><small>Erreurs à reprendre</small><div class="num">${s.seen-s.correct}</div></div></div><div class="panel"><h2>Historique des 40 dernières séances</h2>${state.history.length?state.history.map((h,i)=>`<button class="history-row" onclick="openResult(${i})"><span>${esc(h.title)} · ${new Date(h.date).toLocaleDateString('fr-FR')}</span><b>${h.correct}/${h.total} · ${pct(h.correct,h.total)} % →</b></button>`).join(''):'<p>Aucune séance terminée. Commence par un diagnostic.</p>'}<p>Objectif personnel suggéré : résultats réguliers dans chacun des domaines, puis vitesse. Un bon score sur des questions mémorisées ne garantit pas la réussite.</p><button class="btn" onclick="startErrors()">Revoir les erreurs</button> <button class="ghost" onclick="exportProgress()">Exporter la progression</button><p><label class="ghost" for="importFile">Restaurer une progression</label> <input id="importFile" type="file" accept="application/json,.json" onchange="importProgress(this)"></p><small>Sauvegarde locale à ce navigateur. Aucun compte ni envoi de tes réponses à un serveur.</small></div>`}
function openResult(i){renderResult(state.history[i])}
function exportProgress(){const a=document.createElement('a'),u=URL.createObjectURL(new Blob([JSON.stringify({version:2,state},null,2)],{type:'application/json'}));a.href=u;a.download='ypp-progression.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
async function importProgress(input){try{const f=input.files[0];if(!f)return;if(f.size>2000000)throw Error();const x=JSON.parse(await f.text());if(x.version!==2||!x.state||!Array.isArray(x.state.history)||!Array.isArray(x.state.days)||typeof x.state.items!=='object')throw Error();const clean={items:{},history:[],days:x.state.days.filter(n=>Number.isInteger(n)&&n>=0&&n<12)};for(const q of QUESTIONS){const v=x.state.items[q.id];if(v&&typeof v.ok==='boolean')clean.items[q.id]={ok:v.ok,attempts:Math.max(1,Number(v.attempts)||1),date:String(v.date)}}for(const h of x.state.history.slice(0,40)){if(!Array.isArray(h.rows)||!Number.isFinite(h.total)||!Number.isFinite(h.correct)||!Number.isFinite(h.elapsed)||isNaN(Date.parse(h.date)))throw Error();const rows=h.rows.filter(r=>QUESTIONS.some(q=>q.id===r.id)&&(r.sel===null||Number.isInteger(r.sel)&&r.sel>=0&&r.sel<4)).map(r=>({id:r.id,sel:r.sel,seconds:Math.max(0,Number(r.seconds)||0)}));if(rows.length!==h.total)throw Error();clean.history.push({id:String(h.id),title:String(h.title),date:h.date,exam:!!h.exam,total:rows.length,correct:rows.filter(r=>r.sel===QUESTIONS.find(q=>q.id===r.id).a).length,elapsed:Math.max(0,h.elapsed),rows})}if(!confirm('Remplacer la progression locale par ce fichier ?'))return;state=clean;save();progress()}catch{alert('Fichier de progression invalide. Choisis un export YPP version 2.')}}
function plan(){app.innerHTML=`<div class="panel"><h2>12 séances avant l’ouverture du test</h2><p>Prévois 60 à 90 minutes par séance ; garde 45 minutes pour chaque simulation, puis un temps séparé pour sa correction. Adapte le rythme au temps disponible.</p></div><div class="plan-list">${PLAN.map((text,i)=>`<label class="panel plan-item"><input type="checkbox" ${state.days.includes(i)?'checked':''} onchange="toggleDay(${i},this.checked)"><span><b>Séance ${i+1}</b><br>${text}</span></label>`).join('')}</div><div class="notice">Pour le test officiel : choisis une date dans la fenêtre du 28 septembre au 5 octobre, sans attendre le dernier soir. La BAD indique que le lien sera envoyé avant 8 h 00 GMT le lundi 28 septembre. Une fois lancé, le test de 45 minutes doit être terminé en une seule session. Au Togo, l’heure locale correspond à GMT.</div>`}
function toggleDay(i,on){state.days=on?[...new Set([...state.days,i])]:state.days.filter(d=>d!==i);save()}
function showView(v){if(session){if(session.exam){alert('Ton examen est en cours. Termine-le pour ouvrir les autres sections.');return}if(!confirm('Quitter et enregistrer le bilan de cette séance ?'))return;finish(true)}view=v;document.querySelectorAll('.nav').forEach(b=>b.classList.toggle('active',b.dataset.view===v));$('#pageTitle').textContent=({dashboard:'Tableau de bord',learn:'Fiches de révision',news:'Actualités & sources',practice:'Entraînement',exam:'Examen blanc',progress:'Ma progression',plan:'Programme personnel'})[v];({dashboard,learn,news,practice:practiceHome,exam:examHome,progress,plan})[v]();window.scrollTo(0,0)}
document.querySelectorAll('.nav').forEach(b=>b.onclick=()=>showView(b.dataset.view));
const pending=read(SESSION,null);
if(pending&&Array.isArray(pending.qs)&&pending.qs.length&&pending.qs.every(q=>QUESTIONS.some(x=>x.id===q.id)&&Array.isArray(q.order))&&pending.answers&&pending.checked&&pending.times&&pending.flagged){session=pending;session.entered=Date.now();if(state.history.some(h=>h.id===session.id)){session=null;saveSession();dashboard()}else if(session.exam&&remaining()===0)finish(true);else{renderQ();clock()}}else dashboard();
